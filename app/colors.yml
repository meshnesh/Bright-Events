Example endpoint returning a list of colors by palette
In this example the specification is taken from external YAML file
---

parameters:
  - in: formData
    name: email
    type: string
    required: true
    description: Requires a valid email address
  - in: formData
    name: name
    type: string
    required: true
    description: Accepts an alphanumeric username
  - in: formData
    name: password
    type: string
    required: true
    description: Contains lowercase, uppercase and digits, and at least 6 characters
responses:
        201:
          description: Registration of User
          schema:
            $ref: '#/user/registration'
          examples:
            userID: {'name':"Antony", 'email':"tonny.nesh@gmail.com", 'password':"qwerty123"}